export class UFSMCourse {
    id: number;
    name: string;
    site: string;
  
    constructor(id: number, name: string, site: string) {
      this.id = id;
      this.name = name;
      this.site = site;
    }
}